@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer localizer

<!DOCTYPE html>
<html>
    <head>
        <partial name="_Head" />
    </head>

    <body class="sb-sidenav-toggled">
        <div class="d-flex" id="wrapper">
            <div class="border-end bg-white" id="sidebar-wrapper">
                <div class="sidebar-heading border-bottom bg-light">@localizer["HelpPage"]</div>
                <div class="list-group list-group-flush">
                    <p>@localizer["Add"]</p>
                    <p>@localizer["Modify"]</p>
                    <p>@localizer["Delete"]</p>
                </div>
            </div>
            <form method="post" style="width:100vw">
                <partial name="_Header" />
                <div class="container">
                    <main role="main" class="pb-3">
                        <ul class="nav nav-tabs">
                            <li class="nav-item">
                                <a id="Home" class="nav-link" href="#">Home</a>
                            </li>
                            <li class="nav-item">
                                <button type="submit" class="nav-link" asp-controller="Administrator" asp-action="Accounts">@localizer["Administrator"]</button>
                            </li>
                            <li class="nav-item">
                                <a id="Users" class="nav-link" href="#">Users</a>
                            </li>
                        </ul>
                        @RenderBody()
                    </main>
                </div>
                <partial name="_Footer" />
            </form>
            <partial name="/Views/Shared/_Modal.cshtml" />
        </div>
    </body>
</html>